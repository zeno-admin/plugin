# {{ title }}

`{{ package }}` 是一个纯后端 Zeno Admin 插件。Composer 安装后，插件 key 为 `{{ key }}`，运行目录为 `plugins/{{ key }}`。

## 本地开发

在宿主项目 `composer.json` 的 `repositories` 中加入本地 Path Repository：

```json
{
    "type": "path",
    "url": "packages/{{ key }}",
    "options": {
        "symlink": true
    }
}
```

随后在宿主项目根目录安装并初始化插件：

```bash
composer require {{ package }}:@dev
php artisan migrate --no-interaction
```

`migrate` 成功后会执行 `install`、`enable` Hook，并启用插件。

修改 PHP 类后执行：

```bash
composer dump-autoload
```

## License

[MIT](LICENSE)
